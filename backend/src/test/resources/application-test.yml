jdbc:
   driverClassName: org.h2.Driver
   url: jdbc:h2:mem:asiproject;DB_CLOSE_DELAY=-1

spring:
   jpa:
      hibernate:
         dialect: org.hibernate.dialect.H2Dialect
         ddl-auto: create-drop